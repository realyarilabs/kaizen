<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Kaizen" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <!-- Google font Inter-->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400&display=swap" rel="stylesheet">

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <script src="https://cdn.tailwindcss.com"></script> <!-- CDN do Tailwindcss -->
    <script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.turbo.min.js"></script> <!-- CDN do Flowbite -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render "layouts/navbar" %>
    <div id="flash">
      <%# <%= render "layouts/flash" %>
    </div>
    <%= render "layouts/sidebar" %>
    <div class="p-4 bg-gray-100 sm:ml-64">
      <!-- Main content container-->
      <main class="p-4 border-2 border-gray-200 border-dashed rounded-lg mt-14">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
